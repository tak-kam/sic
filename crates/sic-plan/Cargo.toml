[package]
name = "sic-plan"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sic-core = { path = "../sic-core" }
sic-bytecode = { path = "../sic-bytecode" }
sic-json = { path = "../sic-json" }
# The flow analysis lives in the verifier, which is the crate whose job is
# deciding what is safe to run. A plan reports what it finds; the verifier
# refuses it. Two readers of one analysis, and this is the direction the
# dependency has to point.
sic-verify = { path = "../sic-verify" }

[lints]
workspace = true
